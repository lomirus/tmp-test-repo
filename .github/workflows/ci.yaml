on: push

name: build

jobs:
  chechout:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: cargo build --release
      - uses: actions/upload-artifact@v2
        with:
          name: build
          path: target/release
